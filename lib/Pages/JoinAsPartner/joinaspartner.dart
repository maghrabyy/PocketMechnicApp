import 'package:flutter/material.dart';

class JoinAsPartner extends StatelessWidget {
  static const String id = 'JoinAsPartner';
  const JoinAsPartner({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
