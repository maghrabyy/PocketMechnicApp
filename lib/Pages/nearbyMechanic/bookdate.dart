import 'package:flutter/material.dart';

class MechanicBookDate extends StatelessWidget {
  const MechanicBookDate({Key? key}) : super(key: key);
  static const String id = 'MechanicBookDate';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
